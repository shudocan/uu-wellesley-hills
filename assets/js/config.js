/* config.js — site-wide config that's safe to expose publicly.
   Set the Formspree endpoint here to enable the Rental Inquiry form to email
   the office. Create a free form at https://formspree.io (point it at
   info@uuwellesley.org), then paste its endpoint URL below.
   While this is empty, the form gracefully falls back to opening the
   visitor's email app pre-filled. */
window.UU_FORM_ENDPOINT = ""; // e.g. "https://formspree.io/f/abcdwxyz"
